description: The `DrsObjects` were found successfully
content:
  application/json:
    schema:
      type: object
      properties:
        drsobjects:
          type: array
          items:
            type:
              $ref: '../schemas/DrsObject.yaml'
