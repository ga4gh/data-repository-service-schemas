type: object
required: 
  - cloud_provider
properties: 
  cloud_provider:
    type: string
    enum: 
      - aws
      - gcp
      - azure
    description: specify cloud provider for compute
  cloud_region:
    type: array
    items: string
    description: specify cloud regions for compute
    example: us-east-1
    
