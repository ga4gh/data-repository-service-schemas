type: object
required:
  - checksums
properties:
  checksums:
    type: array
    items:
      $ref: './Checksum.yaml'
    minItems: 1
    description: New checksums for the DRS object
  passports:
    type: array
    items:
      type: string
    description: Optional GA4GH Passport JWTs for authorization